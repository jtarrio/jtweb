<!DOCTYPE html>
<html>
    <head>
        <title>
            {{if .Tag}}
                {{if .Year}}Historias etiquetadas &ldquo;{{.Tag}}&rdquo; en {{.Year}}{{end}}
                {{if not .Year}}Historias sen data etiquetadas &ldquo;{{.Tag}}&rdquo;{{end}}
            {{end}}
            {{if not .Tag}}
                {{if .Year}}Historias publicadas en {{.Year}}{{end}}
                {{if not .Year}}Historias sen data{{end}}
            {{end}}
        </title>
        <link rel="stylesheet" href="{{webRoot}}/style/style.css">
    </head>

    <body>
        <div class="titleBar">
            <div class="title">
                {{if .Tag}}
                    {{if .Year}}Historias etiquetadas &ldquo;{{.Tag}}&rdquo; en {{.Year}}{{end}}
                    {{if not .Year}}Historias sen data etiquetadas &ldquo;{{.Tag}}&rdquo;{{end}}
                {{end}}
                {{if not .Tag}}
                    {{if .Year}}Historias publicadas en {{.Year}}{{end}}
                    {{if not .Year}}Historias sen data{{end}}
                {{end}}
            </div>
            <div class="count">
                {{if .Year}}{{.YearCount}} {{plural .YearCount "historia" "historias"}} en {{.Year}},{{end}}
                {{if not .Year}}{{.YearCount}} {{plural .YearCount "historia" "historias"}} sen data,{{end}}
                {{.TotalCount}} {{plural .TotalCount "historia" "historias"}} en total.
            </div>
        </div>

        <div class="indexBox">
        {{range .Stories}}
            <div class="indexItem">
                <div class="title"><a href="{{.Permalink}}">{{.Title}}</a></div>
                {{if .PublishDate}}<div class="date">{{formatDate .PublishDate}}</div>{{end}}
                {{if .Summary}}<div class="summary">{{.Summary}}</div>{{end}}
                {{if .Tags}}<div class="tags">Sobre {{range $i, $e := .Tags}}{{if $i}}, {{end}}&ldquo;<a href="{{getTagURIWithYear $e $.Year}}">{{$e}}</a>&rdquo;{{end}}.</div>{{end}}
            </div>
        {{end}}
        </div>

        <div class="theEnd">❧ ☙</div>

        <div class="footBar">
            {{if .StoryYears}}<div>Outras historias escritas en {{range $i, $e := .StoryYears}}{{if $i}}, {{end}}<a href="{{$.BaseURI}}-{{$e}}.html">{{$e}}</a>{{end}}.</div>{{end}}
            {{if .UndatedStories}}<div>Outras <a href="{{.BaseURI}}-0.html">historias sen data</a>.</div>{{end}}
            <div>Índice <a href="{{getTocURI}}">completo</a>.</div>
            {{if .Tag}}<div>Índice de &ldquo;<a href="{{getTagTocURI .Tag}}">{{.Tag}}</a>&rdquo;.</div>{{end}}
        </div>
        <div class="bottomBar">Agás que se indique o contrario, esta páxina e o seu contido son Copyright &copy; Jacobo Tarrío Barreiro. Todos os Dereitos Reservados. <a href="{{webRoot}}/terms-gl.html">Información sobre tratamento de datos e condicións de uso.</a></div>
    </body>
</html>
